{
  "name": "code-zone",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "code-zone-frontend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm start --prefix frontend",
    "dev": "concurrently \"cd backend && npm run server \" \"cd code-zone-frontend && npm run start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "axios": "^1.7.7"
  }
}
